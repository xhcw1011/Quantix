# WebSocket upgrade-aware Connection header. Loaded by /etc/nginx/conf.d/ at
# the http {} level so the sites-enabled/quantix server block can reference
# $quantix_connection_upgrade in proxy_set_header.
map $http_upgrade $quantix_connection_upgrade {
    default upgrade;
    ''      close;
}
